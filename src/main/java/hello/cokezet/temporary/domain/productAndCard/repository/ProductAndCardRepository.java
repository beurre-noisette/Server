package hello.cokezet.temporary.domain.productAndCard.repository;

import hello.cokezet.temporary.domain.productAndCard.entity.ProductAndCard;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProductAndCardRepository extends JpaRepository<ProductAndCard, Long> {

}
